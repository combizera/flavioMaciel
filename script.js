import { Carousel } from "./carousel.js";

const ant = "[data-ant]";
const prox = "[data-prox]";
const listProdutos = "[data-lista-prod]";
const nav = "[data-nav]";

new Carousel(ant, prox, listProdutos, nav);
